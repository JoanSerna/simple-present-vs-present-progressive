import { ButtonCoordDirective } from './button-coord.directive';

describe('ButtonCoordDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonCoordDirective();
    expect(directive).toBeTruthy();
  });
});
